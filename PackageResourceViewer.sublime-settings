{
"ignored_packages"  	: ["Package Control","0_packagesmanager_loader","0_package_control_loader","0_settings_loader"],
  //                	List of packages to ignore, Package Control is ignored by default because extracting it cause errors
"ignore_patterns"   	: ["\\.(git|hg|svn|DS_Store)"],// A list of regex patterns to ignore (compared against the file or directory name)
"open_multiple"     	: true 	,//T Keep selection panel open after selecting a resource to open
"single_command"    	: true 	,//T if a single command should be listed in the command palette for viewing and editing files or if multiple commands should be used
"return_to_previous"	: false	,//F Automatically chose the previous selection when moving up a directory
}
